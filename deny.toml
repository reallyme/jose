[graph]
targets = [
    "x86_64-unknown-linux-gnu",
    "aarch64-unknown-linux-gnu",
    "wasm32-unknown-unknown",
    "aarch64-apple-ios",
    "aarch64-linux-android",
]
all-features = false
no-default-features = false

[output]
feature-depth = 1

[advisories]
ignore = []
yanked = "deny"

[licenses]
confidence-threshold = 0.93
allow = [
    "Apache-2.0",
    "BSD-3-Clause",
    "MIT",
    "Unicode-3.0",
    "Zlib",
]
exceptions = []

[licenses.private]
ignore = true
registries = []

[bans]
multiple-versions = "deny"
wildcards = "deny"
highlight = "all"
workspace-default-features = "allow"
external-default-features = "allow"
# Direct use of broad error/logging crates is prohibited by repository policy
# and source review. Do not ban transitive use unless we are ready to replace
# upstream dependencies that carry them internally.
deny = []
skip = [
    # Buffa 0.9.2 still uses Base64 0.22 while ReallyMe Codec 0.2.3 uses
    # Base64 0.23. Both are current direct dependencies; remove this exact
    # exception when Buffa adopts Base64 0.23 or the dependency trees converge.
    { name = "base64", version = "0.22.1" },
    { name = "getrandom", version = "0.2.17" },
    # Serde 1.0.229 moved its derive implementation to Syn 3 while JNI,
    # zeroize, and WASM proc-macros still require Syn 2. Both versions are
    # compile-time-only; remove this exact exception when those trees converge.
    { name = "syn", version = "2.0.119" },
]
skip-tree = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []

[sources.allow-org]
github = []
gitlab = []
bitbucket = []
